We should specify id-token: write in the caller workflow if you are using workflow_call trigger, even if we have mentioned id-token in callee workflow
always curl the endpoint after 5 sec, cause the application wouldnt be started
