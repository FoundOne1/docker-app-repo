name: Container test
on:
  workflow_dispatch:
    inputs:
      username:
        type: string
        required: true
      registry:
        type: string
        required: true
      registry_address:
        required: true
        type: string
  workflow_call:
    inputs:
      username:
        type: string
        required: true
      password:
        type: string
        required: true
      registry:
        type: string
        required: true
      registry_address:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: pull the image
        id: container-run
        run: |
          echo $GITHUB_TOKEN | docker login ${{inputs.registry}} -u ${{inputs.username}} --password-stdin
          docker pull --name myapp:v1 ${{inputs.registry_address}}
          echo "res=success" >> "$GITHUB_OUTPUTS"
    outputs:
      result: ${{steps.container-run.outputs.res}}
          
    
